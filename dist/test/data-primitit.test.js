"use strict";
describe('Declare type data', function () {
    it('Should must declare', function () {
        const name = 'Bimbim Mulya Krismon';
        const age = 24;
        const isVIP = true;
        console.info(name);
        console.info(age);
        console.info(isVIP);
    });
});
