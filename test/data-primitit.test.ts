describe('Declare type data', function () {
  it('Should must declare', function() {
    const name : String = 'Bimbim Mulya Krismon';
    const age : Number = 24;
    const isVIP : Boolean = true
    
    console.info(name);
    console.info(age);
    console.info(isVIP);
  })
})